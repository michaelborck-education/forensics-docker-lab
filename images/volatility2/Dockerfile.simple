FROM python:3.8-slim

# Install system dependencies
RUN apt-get update && apt-get install -y \
    build-essential \
    python2.7 \
    python-dev \
    python-pip \
    distutils \
    yara \
    libyara3 \
    libyara-dev \
    && rm -rf /var/lib/apt/lists/*

# Install Volatility 2
RUN pip2 install volatility==2.6.1

# Create working directory
WORKDIR /evidence

# Default command
CMD ["vol.py", "-h"]