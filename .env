# Environment variables for Docker Compose
# Note: This file is committed to the repository (contains no secrets)
# In production projects, .env should be in .gitignore

# User IDs for file ownership (Linux/macOS only - Windows doesn't use these)
# Files created in ./cases will be owned by this user
PUID=1000
PGID=1000

# Optional: Display settings for Autopsy GUI via noVNC
DISPLAY_WIDTH=1600
DISPLAY_HEIGHT=968
